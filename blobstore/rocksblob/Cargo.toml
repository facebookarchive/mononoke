[package]
name = "rocksblob"
version = "0.0.1"
authors = ["Facebook"]
license = "GPLv2+"

[dependencies]
bytes = "0.4.5"
error-chain = "0.11.0"
futures = "0.1.17"
rocksdb = "0.8.1"

blobstore = { path = ".." }
